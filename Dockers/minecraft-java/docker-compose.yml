version: "3"

services:
  minecraft:
    image: itzg/minecraft-server
    container_name: mc
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      VERSION: "1.21.11"
      MEMORY: "4G"
    volumes:
      - ./data:/data
    restart: unless-stopped